/**
 *  ********** Bubble Sort **********
 *  1. Push the max element to the end of the array 
 *  by adjust swaps
 */


function swap(arr, i, j) {
    let temp = arr[i];
    arr[i] = arr[j];
    arr[j] = temp;
}


function bubblesort (arr) {
    let n = arr.length;
    for(let i = 0; i)
}